class Imagery:
    # Manage images

    def __init__(self, file_id, payload, camera, zone, settings):
        self.file_id = file_id
        self.payload = payload
        self.camera = camera
        self.zone = zone
        self.settings = settings

